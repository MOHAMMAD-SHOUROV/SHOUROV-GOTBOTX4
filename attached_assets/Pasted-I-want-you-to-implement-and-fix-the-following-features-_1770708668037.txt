I want you to implement and fix the following features in my GoatBot (Goat-Bot-V2 based project).
Node.js version must be 20 (Node 20 compatible).

Please do NOT break existing core logic unless absolutely necessary.

--------------------------------------------------
1) NEW LEVEL SYSTEM (FROM SCRATCH)
--------------------------------------------------

Create a brand new level system file inspired by this reference:
https://files.catbox.moe/lxzpta.gif

Requirements:
- Create a new level system file (clean implementation)
- Level must increase based on message activity
- Rank-up message must send correctly in group chats
- Must NOT throw undefined errors (sendRankup, settings, etc.)
- Level system must work with:
  - handlerEvents.js
  - threadsData
  - usersData
- Rank-up on/off toggle must work
- Rank-up message must be customizable later

--------------------------------------------------
2) s.js FILE MUST WORK
--------------------------------------------------

Fix and ensure:
- s.js loads correctly
- No runtime errors
- Works with handlerEvents.js
- Node.js 20 compatible

--------------------------------------------------
3) VIDEO SYSTEM FILE MUST WORK
--------------------------------------------------

Fix / create video system file:
- Auto video sending must work
- No API crashes
- Support future expansion (adding more videos later)
- Node 20 compatible

--------------------------------------------------
4) love.js COMMAND
--------------------------------------------------

Fix love.js:
- Mention-based replies must work
- Emoji replies must work
- Reply + mention logic must not break
- Works in group chats

--------------------------------------------------
5) emojireply.js
--------------------------------------------------

Fix emojireply.js:
- When specific emoji is sent, bot replies correctly
- Should not conflict with other onChat handlers
- Clean logic

--------------------------------------------------
6) autoreel.js
--------------------------------------------------

Fix autoreel.js:
- Auto reel/video sending must work
- Should be triggered by chat or timer
- No spam
- Node 20 compatible

--------------------------------------------------
7) EMOJI → AUDIO SYSTEM (NEW FILE)
--------------------------------------------------

Create a new file:
- When users send specific emoji or text in group chat
- Bot sends an AUDIO reply

Requirements:
- Emoji/text → audio mapping
- Easy to add more audios later
- Clean structure (array/object based)
- No hardcoded single values

--------------------------------------------------
8) EMOJI/TEXT → VIDEO SYSTEM (NEW FILE)
--------------------------------------------------

Create another new file:
- When users send specific emoji or text
- Bot sends a VIDEO reply

Requirements:
- Emoji/text → video mapping
- Easy to add many videos later
- Supports group chats
- No conflicts with emoji audio system

--------------------------------------------------
GENERAL RULES
--------------------------------------------------

- All new files must be clean and modular
- Must work with handlerEvents.js
- Must not break existing commands
- Proper error handling (no silent crashes)
- Node.js 20 compatibility is mandatory
- I must be able to easily add more videos/audio later by editing arrays

EXPECTED RESULT
--------------------------------------------------

- Level system fully works
- Rank-up notifications work
- s.js works
- video system works
- love.js works with mention + emoji
- emojireply.js works
- autoreel.js works
- Emoji → audio works
- Emoji/text → video works
- No runtime errors
- Stable bot on Node.js 20